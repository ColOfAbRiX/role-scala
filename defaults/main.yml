---
# defaults file for scala

# Version Java JDK to install:
scala_jdk_version: '1.8.0'

# Version of Scala to install:
scala_main_version: '2.12.2'

# Version of Scala SBT to install:
scala_sbt_version: '0.13.5'
