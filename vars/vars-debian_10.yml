---
# vars file for scala

distro_name: buster

scala_java_package: "adoptopenjdk-{{ java_jdk_version }}-hotspot"

scala_main_package: "scala-{{ scala_main_version }}"

scala_sbt_package: "sbt-{{ scala_sbt_version }}"
